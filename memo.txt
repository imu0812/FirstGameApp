之後新增技能時，最實用的 prompt 應該改成這種

不要再下「幫我優化 test mode」這種大 prompt。
改成每次功能新增時，順手要求它補測試資料。

你可以直接用這個模板：

這次新增了新技能 / 新怪物，請只做 test mode 相關的最小補充，不要重構既有架構。

需求：
1. 確認新的資料定義可自動出現在 TestModePanel
2. 若有需要，新增 1~2 個對應的 test preset
3. 不要修改正式遊戲主流程
4. 不要改動既有 test mode 架構
5. build 必須可過

請最後輸出：
- 哪些新資料已自動被 panel 讀到
- 新增了哪些 preset
- 是否有任何相依風險